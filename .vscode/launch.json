{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Live Server",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/node_modules/live-server/live-server.js",
      "args": [
        "--port=3000",
        "--host=localhost",
        "--no-browser"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
